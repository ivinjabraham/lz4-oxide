/target

# Transient output of `make abi-check` (the Rust side of the comparison).
# The committed abi.txt is the reference contract and IS tracked.
/abi.rust.txt

*.o
*.a

__pycache__/
*.pyc

# Raw test-suite logs: megabytes of datagen progress bars. The distilled
# results are committed (bench/baseline.md, bench/results.json, fuzz/log.txt).
/bench/*.log

